{
  "lexicon": 1,
  "id": "fm.leadsheet.sheet",
  "description": "A chord sheet or tab for a song, written in ChordPro.",
  "defs": {
    "main": {
      "type": "record",
      "description": "A chord sheet or tab for a song, written in ChordPro.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": [
          "title",
          "artist",
          "content",
          "createdAt"
        ],
        "properties": {
          "title": {
            "type": "string",
            "maxLength": 1000,
            "maxGraphemes": 200,
            "description": "Song title."
          },
          "artist": {
            "type": "string",
            "maxLength": 1000,
            "maxGraphemes": 200,
            "description": "Performing artist, as credited on the recording this sheet follows."
          },
          "album": {
            "type": "string",
            "maxLength": 1000,
            "maxGraphemes": 200
          },
          "kind": {
            "type": "string",
            "maxLength": 64,
            "knownValues": [
              "chords",
              "tab",
              "ukulele",
              "bass",
              "piano"
            ],
            "default": "chords",
            "description": "What the sheet is for: chords over lyrics (guitar), guitar tablature, ukulele chords, bass, or piano."
          },
          "format": {
            "type": "string",
            "maxLength": 64,
            "knownValues": [
              "chordpro"
            ],
            "default": "chordpro",
            "description": "Markup of `content`. ChordPro: inline [C]hords, {directives}, and {start_of_tab} blocks."
          },
          "content": {
            "type": "string",
            "maxLength": 100000,
            "maxGraphemes": 50000
          },
          "key": {
            "type": "string",
            "maxLength": 16,
            "description": "Key the sheet is written in, e.g. 'G' or 'F#m'."
          },
          "capo": {
            "type": "integer",
            "minimum": 0,
            "maximum": 12
          },
          "tuning": {
            "type": "string",
            "maxLength": 64,
            "knownValues": [
              "standard",
              "eb-standard",
              "d-standard",
              "drop-d",
              "drop-c",
              "dadgad",
              "open-d",
              "open-g",
              "open-e",
              "open-c",
              "low-g",
              "baritone",
              "five-string"
            ],
            "description": "How the instrument (see `kind`) is tuned. Unset or 'standard' is that instrument's standard: EADGBE guitar, GCEA ukulele, EADG bass."
          },
          "difficulty": {
            "type": "string",
            "maxLength": 32,
            "knownValues": [
              "beginner",
              "intermediate",
              "advanced"
            ]
          },
          "description": {
            "type": "string",
            "maxLength": 10000,
            "maxGraphemes": 3000,
            "description": "Author's notes: strumming pattern, how the intro is played, etc."
          },
          "links": {
            "type": "array",
            "maxLength": 5,
            "items": {
              "type": "string",
              "format": "uri",
              "maxLength": 2000
            },
            "description": "Where to hear the recording this sheet follows: YouTube, Bandcamp, SoundCloud, a streaming service. http(s) URLs."
          },
          "tags": {
            "type": "array",
            "maxLength": 10,
            "items": {
              "type": "string",
              "maxLength": 640,
              "maxGraphemes": 64
            }
          },
          "voicings": {
            "type": "array",
            "maxLength": 100,
            "items": {
              "type": "ref",
              "ref": "#voicing"
            },
            "description": "Chord shapes the author chose, at most one per chord. Readers see these instead of the shapes the app would pick."
          },
          "draft": {
            "type": "boolean",
            "default": false,
            "description": "Not published yet: apps list and search only published sheets, and show a draft only to its author. Like any record, a draft is still publicly readable from the author's repo."
          },
          "forkOf": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef",
            "description": "The sheet this one was derived from, as it was when forked."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          },
          "updatedAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    },
    "voicing": {
      "type": "object",
      "description": "How to play one chord of the sheet.",
      "required": [
        "chord",
        "frets"
      ],
      "properties": {
        "chord": {
          "type": "string",
          "maxLength": 64,
          "description": "The chord as written in `content`, e.g. 'C' or 'F#m7'."
        },
        "frets": {
          "type": "array",
          "minLength": 1,
          "maxLength": 12,
          "items": {
            "type": "integer",
            "minimum": -1,
            "maximum": 24
          },
          "description": "Fret for each string, in chart order (low E side first on guitar and bass, G first on ukulele), in the sheet's tuning and relative to its capo: -1 = not played, 0 = open."
        }
      }
    }
  }
}
